# Brainstorming visual-companion scratch (not part of the site)
.superpowers/

# OS / editor
.DS_Store
*.swp

# Node (only if a local toolchain is added later)
node_modules/

# Secrets — never commit your API token
.env

# Local Claude Code config / preview scratch
.claude/
